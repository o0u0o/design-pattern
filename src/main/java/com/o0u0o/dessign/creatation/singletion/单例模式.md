# 单例模式
单例是一个思想
## 单例模式应用场景

### 什么场景用到单例模式？
- 多线程中的线程池
- 数据库的连接池
- 系统环境信息
- 上下文(ServletContext)
- ......

### 面问题：
#### 1、系统环境信息
#### 2、Spring中怎么保持组件单例的？
#### 3、ServletContext是什么？是单例的么？怎么保证？
Servlet上下文，封装Servlet的信息，tomcat启动是会启动一个实例

#### 4、ApplicationContext是什么？是单例的么？怎么保证？
- 对tomcat服务器来说，每部署一个应用（一个war包），会有一个应用上下文
- 对Spring容器来说，表示整个IOC容器（只要一个ioc容器，容器相当于组件池），ioc容器中有很多组件（也是单里的）。

#### 5、数据库连接池一般是怎么创建出来的？怎么保证单实例？

